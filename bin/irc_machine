#!/usr/bin/env ruby

require File.join(File.dirname(__FILE__), "../lib/irc_machine")

server = ARGV[0] || raise("Specify a server")
port = 6667

IrcMachine::Session.new(server: server, port: port).start
